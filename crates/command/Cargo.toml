[package]
name = "swactor-command"
version = "0.1.0"
edition = "2024"

[dependencies]
swactor = { path = "../..", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
