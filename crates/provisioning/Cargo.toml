[package]
name = "provisioning"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
swactor = { path = "../.." }
swactor-engine = { path = "../engine" }

[dev-dependencies]
parking_lot = "0.12"
serde_json = "1"
