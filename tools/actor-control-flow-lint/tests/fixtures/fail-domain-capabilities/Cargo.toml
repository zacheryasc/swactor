[package]
name = "actor-lint-domain-fail"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
swactor-engine = { path = "../../../../../crates/engine" }
tokio = { version = "1", features = ["rt", "time"] }

[workspace]
