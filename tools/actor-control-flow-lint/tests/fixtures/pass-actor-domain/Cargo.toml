[package]
name = "actor-lint-domain-pass"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
swactor = { path = "../../../../.." }
swactor-engine = { path = "../../../../../crates/engine" }

[workspace]
